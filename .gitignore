.DS_Store
.vscode
node_modules
dist
*.ipynb
*.pdf
# Local Netlify folder
.netlify
coverage
temp/*
src/scss/*.css
src/scss/*.css.map
# Generated SVG scores (ticket #318): build artefacts produced from
# src/lilypond/ by build/buildScores.mjs. Locally regenerate with
# `npm run build:scores`; Netlify regenerates them via install-lilypond.sh.
src/scores/
# Netlify SVG-cache markers (ticket #423), written by plugins/cache-scores at
# build time: the key marker the plugin compares against the current input key,
# and the cache-hit flag the build command branches on.
.scores-cache-key
.netlify-scores-hit
src/ohmjs/ly-grammar.ohm-bundle.*
publish.py
playwright-output/
playwright-report/
playwright/.cache/

__pycache__/
AGENTS.md
scripts/
