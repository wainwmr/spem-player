name: Monitor Resources Tests

on:
  push:
    branches: [main]
    paths:
      - "packages/monitor/monitor-resources.mjs"
      - "packages/monitor/monitor-resources.test.mjs"
      - "packages/monitor/render-burndown.mjs"
      - "packages/monitor/render-burndown.test.mjs"
      - ".github/workflows/monitor-resources-test.yml"
  pull_request:
    branches: [main]
    paths:
      - "packages/monitor/monitor-resources.mjs"
      - "packages/monitor/monitor-resources.test.mjs"
      - "packages/monitor/render-burndown.mjs"
      - "packages/monitor/render-burndown.test.mjs"
      - ".github/workflows/monitor-resources-test.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"
          cache: pnpm
      - run: pnpm install --filter @spem/monitor
      - run: pnpm --filter @spem/monitor test
